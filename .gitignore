# Dependencies
node_modules/

# Android build artifacts (android/.gitignore covers the rest)
android/app/build/
android/.gradle/
android/local.properties
android/.idea/
*.iml

# Signing keys — never commit keystores
keystore.jks
*.keystore
*.p12
keystore-credentials.txt

# OS / editor noise
.DS_Store
*.log
